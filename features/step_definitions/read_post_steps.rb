require 'watir-webdriver'

When(/^I click the post title$/) do
  pending
end

Then(/^the browser navigates to the post page$/) do
  pending
end

When(/^I am on the post page$/) do
  pending
end

Then(/^the page shows the entire post$/) do
  pending
end

When(/^I view the authors name$/) do
  pending
end

Then(/^the authors name is not null$/) do
  pending
end

When(/^the browser displays the post page$/) do
  pending
end

Then(/^the homepage button is available$/) do
  pending
end